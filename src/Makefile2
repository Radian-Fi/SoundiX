IDIR=../include
CXX=g++
CXXFLAGS=-I$(IDIR)

ODIR=obj
LDIR=../lib

LIBS=-lsndfile

_DEPS = lsndx.h
DEPS = $(patsubst %,$(IDIR)/%,$(_DEPS))

_OBJ = Open2.o freq2midi.o FFT2.o
OBJ = $(patsubst %,$(ODIR)/%,$(_OBJ))

$(OBJ): $(ODIR)/%.o: %.cpp $(DEPS)
	$(CXX) -std=c++11 -c -o $@ $< $(CXXFLAGS)

SoundiX: $(OBJ)
	g++ -std=c++11 -o $@ $^ $(CXXFLAGS) $(LIBS)

.PHONY: clean

clean:
	rm -f $(ODIR)/*.o *~ core $(INCDIR)/*~ 
